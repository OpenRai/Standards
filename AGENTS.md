# Agent Instructions

## Commands

- Always use a Makefile target to start repository tools and services.
- Do not invoke `uv run` directly unless explicitly debugging.
- If a required Makefile target is missing, add or repair it instead of bypassing the Makefile.
